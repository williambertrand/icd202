using UnityEngine;

namespace UI
{
    /**
     * This class is only used to add a type to each menu canvas so they're easily searchable.
     */
    public class CanvasController : MonoBehaviour
    {
        public CanvasType canvasType;
    }
}
